<?xml version="1.0" encoding="utf-8"?>
<!-- "Goals" (Vague 4 du plan de migration, item 14) - une carte par objectif. Pas de
     android:progressTint ici (attribut API 21+, minSdk de l'app = 16) : la couleur de
     la ProgressBar est appliquee par code dans GoalsAdapter
     (getProgressDrawable().setColorFilter(...)), meme motif de "tinting compatible"
     que ViewCompat.setBackgroundTintList() deja utilise ailleurs dans l'app. -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardview_goal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="6dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/goal_row_exercise_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="Exercise Name"
                android:textColor="@color/colorPrimary"
                android:textSize="18sp" />

            <TextView
                android:id="@+id/goal_row_type"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Max Weight"
                android:textColor="@color/core_black" />
        </LinearLayout>

        <ProgressBar
            android:id="@+id/goal_row_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:max="100"
            android:progress="0" />

        <TextView
            android:id="@+id/goal_row_progress_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="end"
            android:text="0 / 0 (0%)"
            android:textSize="12sp" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
