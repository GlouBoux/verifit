---
description: Liste des chantiers en cours sur FitNotes_Fork (fork de verifit) — état au fil des sessions.
---

# TODO — FitNotes_Fork

## En cours

- [ ] **L'app se rouvre toujours sur le jour actuel** : au lancement, `MainActivity.initViewPager()` centre systématiquement le ViewPager sur aujourd'hui (`// Navigate to today`), sans mémoriser le dernier jour consulté. Romain ne veut pas de ce comportement. À préciser la prochaine fois : que doit-il se passer à la place ? (rouvrir sur le dernier jour consulté ? autre chose ?)
- [ ] **Commentaire par série, pas juste par exercice** : `WorkoutSet` a bien un champ `comment` individuel dans le modèle, mais la seule UI existante ("Exercise Comments" dans `AddExerciseActivity`) écrit le **même** texte sur toutes les séries de l'exercice du jour (`saveComment()` boucle sur tous les sets et leur applique le même commentaire) — ce n'est donc pas un vrai commentaire par série aujourd'hui. Confirmé par Romain. À faire : ajouter une UI par ligne de série (ex. bouton/dialog sur chaque `workout_set_row.xml` via `WorkoutSetAdapter`) qui édite le commentaire d'une série précise, sans écraser celui des autres séries du même exercice.

## Fait / validé

- [x] Choix du dépôt de base : `MakisChristou/verifit`.
- [x] Feature "Dupliquer un exercice" — codée, testée sur l'app, fonctionne.
- [x] Feature "Import Session" (JSON) — codée, testée sur l'app, fonctionne.
- [x] App qui build et se lance sur émulateur (après correctifs `mavenCentral()` +
  drawable manquant).
- [x] Bug poids/répétitions inversés après import CSV réel — corrigé (ordre des colonnes
  du CSV aligné sur le format natif de verifit). Confirmé par Romain : le ré-import
  fonctionne.
- [x] Crash "Import failed: IllegalStateException" sur Import Session — corrigé (patch
  #4). Cause confirmée : fausse manip de Romain (mauvais écran / mauvais fichier
  sélectionné dans le picker "Import Session", qui accepte n'importe quel fichier vu
  qu'il utilise le mime type `*/*` — un fichier non-JSON, comme un CSV, y fait échouer le
  parsing Gson avec `IllegalStateException`). Le correctif transforme maintenant ce cas en
  message d'erreur propre au lieu de planter l'app — comportement voulu, rien de plus à
  creuser.
- [x] Rattrapage de l'historique git côté Romain (`git add -A && git commit`, fait depuis
  VSCode) — `git status` propre.

## En attente de décision

- [ ] Pousser la branche `feature/duplicate-exercise-and-session-import` sur `GlouBoux/verifit`, et/ou ouvrir une PR vers `MakisChristou/verifit` en amont — ou rester en local ?
- [ ] Garder, retirer ou ignorer le backend de compte en ligne `verifit_rs` (login, sync WebDAV) ?

## Prochain chantier majeur

- [ ] **Intégrer le générateur de séances (`workout_engine.py`)** : priorité annoncée pour
  bientôt. Condition posée par Romain : le script doit pouvoir évoluer facilement tant
  qu'on est en période de test (donc éviter de figer un couplage rigide trop tôt). Le
  format JSON "Import Session" documenté dans `docs/session-import-format.md` est déjà
  prévu comme point d'intégration ; reste à voir si `workout_engine.py` peut produire ce
  JSON directement, et si l'import restera manuel ou deviendra plus direct plus tard.

## Pas urgent

- [ ] Upgrade AGP (actuellement 7.4.2, une mise à jour est proposée par Android Studio) — délibérément reporté, pas bloquant.
- [ ] Bug préexistant du dépôt d'origine : l'icône "aujourd'hui" de `MainActivity` ne mène à aucun sélecteur de date (code mort `DatePickerFragment`/`onDateSet`). Contournable via l'onglet Diary → jour, donc pas urgent.